# 康鹭记忆 · 终版验收对照清单

> 用途：把项目 brief 的验收要求逐条对上**可复查的证据**（实测数据或截图路径），
> 避免"我觉得没问题"式的结论。带 ⏳ 的是还没跑完、需要补的。

## 一、三维街区

| 验收要求 | 证据 | 状态 |
|---|---|---|
| 真实可交互三维街区（Three.js） | 场景自建：约 178 栋建筑（`建筑 178` 由大字面板读出）、drawCalls 284 | ✓ |
| 可旋转 | 派发 PointerEvent 位移 200px → 方位角 33.2° → −26.4°；`lastGesture={"moved":200,"isDrag":true,"wasClick":false}` | ✓ |
| 可缩放 | 滚轮缩放（`minDist 44 / maxDist 440`，俯仰限 25°–70°） | ✓ |
| 可复位 | 「复位视角」实测：默认 `az=33.215° pitch=35.266° dist=210` → 拖动后 `az=-38.29° pitch=53.142°` → 点按钮**精确回到默认基准**。此前失效的真因是 `map3d.js` 把 `initScene` 的 `{api}` 包装当 api 用，`api.resetView` 为 undefined 被静默跳过 | ✓ |
| 点击地标 | 点标签 → 卡片贴在该地标位置弹出（`left=111 top=40`，在容器内） | ✓ |
| **拖动与点击不冲突（4px 阈值）** | 位移 0px → `{"moved":0,"isDrag":false,"wasClick":true}` 判为点击；位移 200px 判为拖动且**未误触发选中** | ✓ |
| 三核心地标精细 | `buildKangleArch`（立柱/斗拱/竖排楹联真文字贴图/两层额枋匾额）、`buildLujiangArch`（四柱三门+石鼓柱础）、`buildHiringPlaza`（大跨度膜棚顶+吊灯罩+红色招工栏）——聚焦后近景可辨认 | ✓ |
| 其余简化但有高低差与街巷密度 | 建筑 3–8 层（`FLOOR_H` 2.7–3.35，即 9–24 单位）；含街巷、路灯、行道树、绿化带 | ✓ |
| 地点足够多 | **11 处已建三维地标**（3 核心 + 8 新增），另有 4 处外联/服务节点 | ✓ |
| 默认视角标签可见且不重叠 | 大字面板：`标签 总数 12 / 可见 11 / 互相重叠对数 0` | ✓ |
| 转动时标签不闪 | 修复：运动门控（相机位姿指纹比对，运动中不做隐藏决策，停手后 150ms 仍算运动）+ 迟滞结算（连续 3 帧判无位才隐、连续 2 帧判有位才现）+ 位置改每帧更新 + 80ms 位移过渡。**同一套探针各跑 300 次确定性操作：修复前翻转 20 次（hide10/show10）→ 修复后 0 次**；运动中最多同时重叠 0 对，`drawCalls` 仍 284 | ✓ |

## 二、视觉体系

| 验收要求 | 证据 | 状态 |
|---|---|---|
| 纸白 #F1EBDD / 墨灰 #302D28 / 砖红 #B84A3A | `style.css` 的 `:root` 已是该三值；全站硬编码旧色**残留 0 处** | ✓ |
| 六个数字统一砖红 | 首页「一组数字」一行（约1.1 平方公里 / 近2万 / 24小时 / 10万+ / 近350亿元）用 `--red`；原大表格与**无来源的 70% 条形图已移除** | ✓ |
| 宋体标题 + 无衬线正文 | `--serif`（思源宋体/宋体、STSong、SimSun）、`--sans`（思源黑体/苹方/微软雅黑）——**原本就符合**，未改 | ✓ |
| 分级标题大标题明显大 | `--fs-hero` 到 `--fs-h3` 四档（`clamp(54px,8.4vw,122px)` 起） | ✓ |
| 三维场景配色一致 | `scene3d.js` 的 `C` 即 `paper/ink/brick/blue/yellow` 同值 | ✓ |
| （曾尝试后撤销）书法艺术字 | 用户认为难看，且封面背景照片里本就有「康鹭」二字，已全部撤回 | — |

## 三、内容结构

| 验收要求 | 证据 | 状态 |
|---|---|---|
| 首页不繁琐、重点突出 | 从 13 个标题 / 7 块压到 **5 章**：封面（含「点击走进康鹭」入口）→ 简介（含三张现场图）→ **02 三维街区（满幅大图）** → 一组数字（一行）→ 03 制衣的人（四块图）→ 04 产业与变迁（两块大图 + 三张展板） | ✓ |
| 四份人物档案直接可见且可完整阅读 | 首页 4 块以图为主入口（整块可点）→ `people.html` 四份 → `person.html?id=a1…a4` 四段式详情 | ✓ |
| 产业流程互动 | `background.html` 保留区位/产业逻辑/空间形态/乡土词条 4 节（其中含产业四步） | ✓ |
| 变迁记录 | 新建 `chronicle.html`：时间轴六条（倒序），每条带**已发生 / 规划·预期**标签 + 来源行；另含改造与政策、现场材料 | ✓ |
| 关于研究 | `statement.html`：5 节方法 + **参考文献**（原属 background，已归口） | ✓ |
| 内容不丢 | 搬迁表：编年/改造政策/现场材料 → chronicle；参考文献 → statement；`background` 21KB→14KB、`chronicle` 5KB→16KB | ✓ |
| 模块关系清晰 | 「走进康鹭 → 三维街区 → 人物档案 → 产业背景 → 变迁记录 → 关于研究」六项导航；首页章号 01–05 | ✓ |
| 首页左栏空壳 | 已改为**「关系图（平面版）」卡片**：在 `map3d.js` 内重写为只画 SVG 的 `MAP2D`/`buildRelationMap()`/`initRelationMap()`（未引老 `map.js`），底图构件与 1000×680 构图沿用老图，`POIS/LINKS` 仍取自 data.js；viewBox 自适应（实测 313×213）。另给 9 个有三维形体的节点加细环，点它可推三维镜头 | ✓ |
| 外联节点点不动 | 已加 `INFO_EXT`（m10–m15）六条卡片，`placePop` 支持无配图/无引语；chip 加虚线淡底与 ↗ 标记。文案只写「类别 + 方位 + 未做三维重建」，取自 data.js 自带 summary/facts，**未编造**（六条原文已逐条核对）。⚠ 其中「逸景路站」写"在建的地铁 11 号线"——**按 2026 年时间线这句话需要复核**（11 号线一期已于 2024 年底开通）| ✓（一处待核）|

## 四、小白鹭 AI 与联动

| 验收要求 | 证据 | 状态 |
|---|---|---|
| 独立浮动对话窗口 | 沿用旧站点 `ai.js`（右下角浮窗 + 可拖动 + 视线跟随），未改成侧栏 | ✓ |
| 问问这里（地点上下文联动） | 选中地点后提问会带上该地点资料 | ✓ |
| 地图中查看 | 回答含已建模地点时给「地图中查看」按钮，**只执行预定义地点 id**（有白名单保护，未知 id 不聚焦、不抛错） | ✓ |
| 跟随小白鹭（逐站导览） | 4 站：康乐牌坊 → 招工广场 → 鹭江牌坊 → 收尾；按钮文案随站变化、可随时退出；实测无错误 | ✓ |
| 分页相关问题 | 每页预设问题不同（首页/地图/人物/背景/变迁） | ✓ |
| 密钥留服务端 | 线上无密钥 → 走"站内检索模式"并**如实标注**；本机可放 `ai-config.local.js`（不发布） | ✓ |
| 无接口时本地检索可用 | 抽查「小单快反」「招工广场」「旧改到哪一步」均可作答 + 站内跳转；无关问题明确回"没记下来" | ✓ |
| 对话窗遮挡小白鹭 | 改为**底边锚定**（`bottom = window.innerHeight - birdTop + 14`，面板只向上生长，回答再长也不覆盖小鸟）；**2026-09-19 用户实机确认已正常** | ✓ |

## 五、浏览器实测

| 验收要求 | 证据 | 状态 |
|---|---|---|
| 桌面无溢出/遮挡 | 静态核对 7 页 + 多张截图目视 | ✓ |
| 手机端无溢出/遮挡 | 本轮改动后已复测：CSS 390px（物理 780 + DSF2）下渲染 `mob-home.png` / `mob-map.png` / `mob-people.png`；静态核对 7 页无异常、资源齐全、**断链图片 0**。三维页窄屏取景见 `mob-map.png` | ✓ |
| 拖动与点击不冲突 | 见「一」中的 4px 阈值实测 | ✓ |
| 控制台无阻断错误 | 页面兜底未出现"初始化失败"占位；交互项（旋转/4px 阈值/聚焦/复位/标签模式切换/节点索引点击）均已分别实测通过 | ✓ |
| 三维不可用时仍可阅读 | `map3d.js` 失败时显示提示 + 侧栏节点索引与空间关系仍可用 | ✓ |

## 六、交付物

| 要求 | 状态 |
|---|---|
| 源码 | `康鹭记忆-终版\`（7 页 + 4 个 JS + 5 个 CSS + 素材） | ✓ |
| 打包 | `康鹭记忆-终版-交付.zip`（13.0MB） | ✓ |
| 启动说明 | 包内 `启动说明.txt`（双击打开、改内容的位置、打包命令） | ✓ |
| 维护说明 | 包内 `开发与维护笔记.md`（环境三铁律、模块三约束、跨模块保护、诚实性硬线） | ✓ |
| 结构方案 | 包内 `模块结构优化方案.md`（含参考站分析） | ✓ |
| 关键截图 | `sub-places.png`（11 处标签）、`sub-canal.png`（窄巷近景）、`AFTER-drag.png`/`AFTER-after.png`（标签不闪的修复后证据）、`panel-2d.png`（关系图卡片）、`ext-card.png`（外联节点卡片）、`desk-map.png`（通栏整页）、`mob-map.png`（手机端）、`home-final.png`、`page-chronicle.png` | ✓ |
| 实测结果 | 本清单「一」至「五」各行的实测数值与截图路径（旋转/4px 阈值/聚焦/复位/标签闪烁/手机端/内容归口等逐项对应） | ✓ |
| 缺失材料清单 | 见下 | ✓ |

## 七、缺失材料 / 需用户提供

1. **`修改建议\地图\说明.md` 是 0 字节** —— 地标位置只能按公开地图与现场照片推断；页面已标"示意性重建，非测绘成果"。
2. **无受访者肖像授权** —— 四份档案配图一律为环境影像并标注"非本人肖像"。
3. **打版/制衣/出货环节无现场影像** —— 产业四步中两步如实标"暂无现场影像"，未用无关图片充数。
4. **无逐年拆建资料** —— 因此不做逐年拆建动画；变迁记录只收有日期与出处的节点。
5. **未部署** —— 按用户要求未公开部署；线上仍是旧版本。
6. **待用户确认**：① 手机端实机观感；② 对话窗是否仍遮挡小白鹭；③ 转动时标签是否还有闪现。

## 八、保留未动

- 旧站点 `kanglu-dh\`（线上那一版）**一行未改**；原始压缩包 `D:\webDownload\----main.zip` 完好。
- `康鹭记忆-改版\`（按设计方案做的七页新版）、`三维地图试验\`（早期三维试验）原样保留，可作对照。

---

## 九、2026-09-19 用户实机验收后的两轮追加（本清单已合并计入上文）

用户逐轮实机反馈并已闭环的项目：标签闪烁（翻转 20→0）、首页大图重导（标针版）、
数字圆标→**水滴标针**（针尖=锚点，误差 0.06px）、白边（先量后改：白边就是纸白 fill 本身，
已改墨灰实底+纸白数字）、首页左栏空壳→**关系图（平面版）**、6 个外联节点点击有反馈、
复位视角（随 `{api}` 包装 bug 一并修好）、全站字号两轮放大（15→16.5→18px）、
图片组内缩留白 + 两小图竖向与大图上下齐平、变迁记录视觉化（轴线/年份/节点/卡片/引语/配图）、
首页数字六宫格与有来源占比条恢复、工序 01→06 加箭头串联。

**用户已实机确认通过**：小白鹭对话窗不再遮挡右下角的小白鹭（底边锚定修复）。

**本轮仍在改（交付前会再核一遍）**：
1. 工序块第二行"反向阅读"被用户否掉 → 改为两行都从左到右（回折线或 6 步单列）
2. 招工广场**膜棚顶**加肋条/分幅接缝/柱顶支撑等结构细节（只动广场棚顶，不动牌坊函数与配色常量）

---

## 十、2026-09-19 第三轮（图片对齐 / 工序一行 6 列 / 三维大图重导）

| 要求 | 做法 | 复测数据（1680px 视口，无头截图读页面上的探针数值） |
|---|---|---|
| 图片与文字左对齐、左右留白等宽 | 删掉 `.figure-block` 的 `--inset + margin-inline:auto`；图组与正文同处一个盒；新开 `--col-pad` 只作用于整列 | 简介章：正文段 L=258 R=980.1，图组 L=258 R=1422 → **左差 0**；产业章材料三图相对内容列的 `--col-pad` **左差 0 / 右差 0** |
| 两小图竖排、与大图上下齐平（保持） | 不改 | 右列 vs 大图 **上=0 下=0**（简介章、产业章各一组） |
| 工序 01—06 恢复一行 | `.chain` 改为「6 列 + 5 个箭头格」共 11 个网格项；箭头改成真实 DOM 元素 `.chain-arw`（伪元素的百分比偏移在本环境解析不一致） | `cols=11 子项=11 行数=1`；**箭头中线与图标圆心 dCY=0**；箭头两端到相邻图标外缘 **gapL=gapR=94**；圆心间距 202,202,202,202,202 |
| 箭头对准图标圆心 | 箭头格与整行等高，`::after` 用 `top:calc(26px - 5px)` 压到圆心线 | 同上（5 条箭头逐条量，dCY 全为 0） |
| 放不下就压块内字号、不动全站基准 | 仅 `@media (min-width:1081px)` 内 `.chain-step p` 15px / `h5` 19px / `.chain-time` 13px；全站正文仍 18px | 6 列下块宽约 193px（1680 视口） |
| 时间戳保留、不新造内容 | `renderChain()` 仍只用 `data.js` 的 `CHAIN` | `data.js` 与上一版交付包**逐字节相同**（SHA256 一致） |
| 手机端向下箭头 | `≤1080px` 回落单列，`.chain-arw` 隐藏，改由 `.has-arrow::before` 画向下箭头，水平位置与圆心对齐（left:21px） | 390px 视口截图 `view-chain-mobile.png`：单列 + 虚线向下箭头 |
| 首页三维大图重导（标针已改成 22×36） | 只含三维场景的临时导出页，2590×995 窗口（视口 2560×900），`dist=155`，标针临时放大 1.5 倍；导出后裁 2560×900 → 2560 宽 q84 / 1280 宽 q82 | 新 `street-3d.jpg` 321,814B（2560×900）、`street-3d-1280.jpg` 103,720B；`index.html` 里的引用与 `width/height` **未改动** |

**未改动（逐字节核对过）**：`assets/js/data.js`、`map3d.js`、`scene3d.js`（因此 `scene-bundle.js` 无需重打包）、`ai.js`、
`people-data.js`、`map.html`、`people.html`、`person.html`、`statement.html`、`chronicle.html`。
三维模块的配色常量、`FLOOR_H`、`CAM.defPos/defTarget`、两座牌坊建模函数、`map3d.js` 的错误兜底**全部未动**。

---

## 十一、2026-09-19 第四轮（标针白底板 / 箭头居中）

| 问题 | 定位 | 修法 | 复测 |
|---|---|---|---|
| 地图页每枚水滴标针外面有一块白色圆角底板（用户："白色长方形上面有水滴型图案"） | 分三步量出来：① `.k3d-label` computed style 是 `rgba(0,0,0,0)` + 无边框无阴影，排除 button；② 截图放大 4 倍量白块恰好 22×36 = 标针 SVG 的 viewBox；③ 打 `outerHTML` + 逐层 computed style，看到 `<svg class="k3d-pin">` 的 `bg=rgb(241,235,221)` | `style.css` 里 `.map-stage svg{ background:var(--paper); border-radius:2px }` 过宽（本意只给平面图 `#kangluMap2d` 铺垫底，却把三维场景里的标针 SVG 一起刷白）。改为 `#kangluMap2d, .map2d-host svg`，并补 `.map-stage svg.k3d-pin{ background:none; border-radius:0 }` 兜底 | 修后：`.k3d-pin bg=rgba(0,0,0,0) radius=0px`；`#kangluMap2d bg=rgb(241,235,221) radius=2px`（平面图底色保留）；4 倍放大截图里标针已是**纯水滴** |
| 工序箭头夹在两枚图标之间要居中 | 箭头图形 140px 长，原先 `background-position:left` ⇒ 贴着左边那枚图标 | `background-position` 改 **center** | 5 条箭头逐条量：左端到左圆、右端到右圆**各 ≈55px（两侧相等）**；箭头中线与圆心 **dCY=0** |

