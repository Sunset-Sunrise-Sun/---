/* ============================================================
   康鹭记忆 · AI 问答 接口配置
   ------------------------------------------------------------
   本文件会随站点一起发布，因此只放「公开、可公开」的内容：
   代理地址、外观与预设问题。**绝对不要在这里写 API Key。**
   
   Key 请写到同目录下的 ai-config.local.js（该文件已被 .gitignore 忽略，
   不会进入仓库、也不会被部署）。两个文件的配置会自动合并，local 优先。

   线上要让 AI 真正工作，需要先部署一个持 Key 的代理（见 ai-config.example.js
   里的 Cloudflare Worker 示例），然后把地址填到下面的 proxyUrl。
   不填 proxyUrl 时，线上会以「本地检索」模式运行——从站内数据里找答案，
   不调用任何外部接口，也就不会有 Key 泄露的风险。
   ============================================================ */

window.KL_AI = {

  /* 自建代理地址（Cloudflare Worker，Key 存在服务端）。
     ────────────────────────────────────────────────────────────
     重要：**这一行在本地演示时必须是空的**。
     ai.js 的选择逻辑是「有 apiKey 就直连，否则用 proxyUrl」，
     而本机的 ../_ai-config.local.js 里有 apiKey —— 如果这里预填了 proxyUrl，
     本地演示也会被拽去走 Worker，Worker 那边 Key 一旦失效就整站不能答。
     所以：本地留空；**部署上线时再把 Worker 地址填回来**。
     线上（github.io）不会加载 local 配置 → apiKey 为空 → 自动走代理。 */
  proxyUrl:  'https://kanglu-ai.kanglu-memory.workers.dev',
  proxyPath: '/chat',

  /* 人格设定：AI 的身份与说话方式（不填则用 ai.js 内置的小白鹭设定）。
     这里决定回答是「像搜索摘要」还是「像有人讲给你听」，是效果差别最大的一个字段。 */
  // persona: '你叫小白鹭，是……',

  /* ---------- 以下为外观与行为，可按需调整 ---------- */
  title: '康鹭摸摸问',
  intro: '关于康鹭片区，问点具体的。',
  placeholder: '问：这里为什么叫「制衣村」？',
  mascotImg: '',            /* 卡通形象图片路径；留空则用文字按钮 */
  mascotText: '问',
  temperature: 0.3,
  maxTokens: 800,
  presets: [
    '这里为什么叫「制衣村」？',
    '「小单快反」是什么意思？',
    '招工广场是怎么来的？',
    '旧改现在到哪一步了？',
    '受访者一共有几位？他们分别是谁？'
  ]
};
