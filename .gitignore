# 本地研究资料：不外发到公开仓库
资料/

# 原始压缩包：内容已解压为 kanglu-dh/
网页-0910.zip

# 图片素材原始压缩包：处理后的成品已入 kanglu-dh/assets/img/
数字人文图片.zip

# AI 问答的本地密钥配置（含 API Key，绝不入库）
# 注意：ai-config.js 是公开配置，需要入库；只有 .local.js 含密钥
# 新位置（推荐）：项目根目录下，在交付目录之外，避免"整个文件夹发给别人"时连密钥一起发出去
_ai-config.local.js
kanglu-dh/assets/js/ai-config.local.js
康鹭记忆-终版/assets/js/ai-config.local.js

# xlsx 解析与临时脚本
_xlsx_tmp/

# 打包用的临时暂存目录（脚本每次会自动清理；万一中途失败会残留，别提交）
_stage2/

# wrangler 本地密钥文件（wrangler dev / deploy 会读它，含明文 Key，绝不提交）
.dev.vars
worker/.dev.vars

# 系统 / 编辑器临时文件
Thumbs.db
desktop.ini
.DS_Store

# 与本站无关的参考文件（本地保留，不上传）
武英殿网页效果演示.html

# wrangler 的本地缓存与构建产物（含账号信息与临时打包文件）
.wrangler/

# 改版工作目录（独立开发，不混入现有站点）
康鹭记忆-改版/
